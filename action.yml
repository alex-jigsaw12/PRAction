name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  file-name:
    description: file with comment
    required: true
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node16'
  pre: chmod +x ./action/work.sh
  main: 'action/index.js'